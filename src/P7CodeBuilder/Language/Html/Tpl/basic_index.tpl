<!DOCTYPE html>
<html lang="{{LANG}}">
  <head>
    <meta charset="utf-8" />
    <title>{{TITLE}}</title>
    <link rel="stylesheet" href="{{CSS_FILE}}" />
    <link rel="icon" href="{{FAVICON_FILE}}" />
  </head>

  <body>
    <script src="{{ECMA_SCRIPT_FILE}}"></script>
  </body>
</html>